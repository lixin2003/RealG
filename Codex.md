## 项目说明

这是一个参考“槽位三消 + 分层遮挡”玩法的网页样品目录，使用 `E:\codex\GAI` 中提供的图片素材做牌面资源。

## 目录约定

* `index.html`：页面结构
* `styles.css`：主题样式与动画
* `script.js`：游戏逻辑
* `assets/tiles/`：游戏使用的牌面图片副本

## 开发约定

* 先做可玩的样品，再逐步补齐机制
* 不直接改原始素材目录，样品目录使用素材副本
* 关卡与牌组数据化，避免把规则写死在渲染代码里
* 每次改动后至少做静态检查和一次关键流程自查
